import { ICollectionIndex } from '../common/common.interface';

export class AssetService implements ICollectionIndex{
  public addIndex(id: string) {
    // llamar a grapql
    // indexar
  }

  public updateIndex(id: string) {
    // llamar a grapql
    // indexar
  }

  public deleteIndex(id: string) {
    // llamar a grapql
    // indexar
  }

  public reIndex() {
    // llamar a grapql
    // indexar
  }
}
